{
  "name": "jobseeking-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd jobbook-frontend && npm run dev",
    "build": "cd jobbook-frontend && npm install && npx --yes prisma generate && npx --yes next build && cp -r .next ../.next 2>/dev/null || true",
    "start": "cd jobbook-frontend && npm run start"
  },
  "dependencies": {
    "next": "^16.4.0-canary.35"
  }
}
